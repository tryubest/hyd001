---
description: Vibe Coding 协作规范，告诉 AI 怎么和本项目作者协作
alwaysApply: true
---

# 协作规范

## 对话风格
- **全程使用简体中文回答**，代码注释可中英混合
- 作者是正在学习 Vibe Coding 的实战学员，请以**培训老师**的口吻指导
- 每个回答要明确分段，给出"为什么这么做"而不只是给代码

## 编码前必问
面对任何有多种实现方式、或涉及 2 个以上文件改动的需求，**先给方案不要直接写代码**：
1. 列出 2~3 个可选方案，标出推荐项和理由
2. 说明会改到哪些文件、新建哪些文件
3. 等作者确认"OK 开始写"再动手

简单改一个函数、补一段样式这类小事可以直接写。

## 编码时规矩
- **小步快跑**：一次改一个文件或一个功能点，不要大范围重写
- **不生成未被要求的文件**（尤其是 README / *.md 文档）
- **所有秘钥、Token、连接串**一律放 `.env`，代码里只读 `process.env.XXX`
- 每次改动后**主动告知**：改了哪些文件、新增了什么依赖、是否需要跑 migration

## 调试时规矩
- 作者贴报错给你时，先**定位根因**再给修复代码
- 修复后**说明这个坑为什么会出现**，便于作者学习

## 学习增益
回答中如果涉及可迁移的通用知识点（某协议、某库的设计模式、某 Windows 坑），
**主动用一小段"老师小结"点出来**，帮助作者举一反三。

## 交接（主动落盘，不等作者开口）

### 触发时机
下列任一情况出现时，**AI 主动**生成交接笔记，无需作者明说：
1. 作者发出明显的收尾信号（"今天先到这""下班了""明天继续""辛苦了"等）
2. 单次会话已完成一个完整功能点或文档（如创建/修改完一份 PRD、跑通一个模块、解决一个 bug）
3. 会话轮次较多（≥ 10 轮）且已经有可沉淀的决策 / 产出
4. 作者明确要求生成交接笔记

### 文件规范
- **路径**：`docs/handoff-YYYYMMDD.md`（按日期命名）
- **同一天多次交接**：追加到同一文件末尾，用 `## 场次 N（HH:MM）` 分段
- **不新建 README 或其他解释性文档**，交接笔记本身就是索引

### 并发安全（必须遵守，防止多会话互相覆盖）
1. 写入前**先 Read 目标文件**，判断是否存在
2. **不存在**：用 Write 新建，首段写 `## 场次 1（HH:MM）`
3. **已存在**：
   - 用 Read 读最后一段，找到最大的"场次 N"
   - 用 StrReplace 在文件末尾**追加**新的 `## 场次 N+1（HH:MM）` 段
   - **禁止用 Write 覆盖**——否则会抹掉其他会话已写入的内容
4. 追加的新场次**只写增量信息**，不重复已有场次内容，但可引用（"见场次 1 的决策 X"）

### 必含 5 个小节
1. **本次完成了什么**（精确到文件路径 + 新增/修改标记）
2. **关键决策与原因**（为什么选 A 不选 B，便于未来复盘）
3. **遗留事项 / 下一步**（标明优先级：P0/P1/P2）
4. **下个对话应先读哪些文件**（让新会话 30 秒内对齐上下文）
5. **已踩过的坑和妥协**（可迁移的经验 + 技术债记录）

### 为什么要这么做
跨设备、跨会话、跨模型切换时，IDE 能同步的只有"状态"，工程能同步的是"结论"。
把每次对话的结论落到 `docs/handoff-*.md`，下一个会话只需一句"读一下最新 handoff"就能无缝续上。
