# 交接笔记 · 2026-04-20

> 项目启动首日。主要交付：PRD v1.1 + 协作规则升级。

---

## 场次 1（PRD 初版 & 战略迭代）

### 1. 本次完成了什么

| 操作 | 文件路径 | 说明 |
| --- | --- | --- |
| 新增 | `docs/PRD.md` | PRD v1.0 初版，10 个章节，涵盖画像/故事/功能/流程图/非功能/竞品/里程碑/风险/开放问题 |
| 修改 | `docs/PRD.md` | v1.0 → v1.1：补充画像 C 参与路径、US-16 Fork-first、F-24~F-26、7.4 GitHub 官方竞品分析、9 战略风险与版权风险 |
| 修改 | `.cursor/rules/vibe-coding.mdc` | 交接规则从"被动响应"升级为"主动落盘"，明确触发时机、文件命名、5 段结构 |
| 新增 | `docs/handoff-20260420.md` | 本文件，按新规则自动生成 |

**未改代码、未装依赖、无需跑 migration。** 项目目前仅有 `.cursor/` 和 `docs/` 两个有效目录，还没有 Next.js 脚手架。

### 2. 关键决策与原因

| 决策 | 选择 | 为什么 |
| --- | --- | --- |
| MVP 用户范围 | **方案甲 Owner-Only**（仓库所有者才能触发翻译） | GitHub App 授权方向单向，任意用户翻译他人仓库在 GitHub 权限模型下走不通；方案甲最简单且零滥用风险 |
| 画像 C 的 P2 路径 | **Fork-first + 只读目录站并行** | 既给画像 C 真实贡献通道（fork），又给 SEO 引流入口（目录站），两者互相导流形成增长闭环 |
| 护城河设计 | **数据资产（TM）+ 多平台适配器 + 垂直运营** | 对抗"GitHub 自己下场做翻译"的平台风险；F-25/F-26 专门为此埋点 |
| 技术栈 | **严格遵守 `project-context.mdc`**（Next.js 15 / Neon / Prisma / GitHub App / OpenRouter） | 不擅自更换，规则文件已锁定 |
| 交接笔记规范 | **主动生成而非被动响应** | Vibe Coding 的跨设备/跨会话场景下，"结论落盘"比"状态同步"更可靠 |

### 3. 遗留事项 / 下一步

**P0（下次会话优先处理）**

- [ ] 决定 `docs/PRD.md` 第 10 章 4 个开放问题：
  1. 是否允许私有仓库接入？（倾向：仅公开）
  2. 术语表放仓库还是平台？（倾向：仓库里 `.github/i18n.yaml`）
  3. 翻译 PR 同分支追加还是新开？（倾向：同分支追加）
  4. 免费额度设定？（待定）
- [ ] 启动 M0 脚手架：`pnpm create next-app` + Prisma + Neon + shadcn + GitHub OAuth 跑通

**P1（1~2 周内）**

- [ ] 在 GitHub 注册一个测试用 GitHub App，拿到 App ID / 私钥 / Client Secret，写入 `.env.local`（不进 git）
- [ ] 跑通 cloudflared 本地 Webhook 调试链路
- [ ] 实现最小可用的 Markdown 翻译（调 OpenRouter `:free` 模型，保护代码块）

**P2（视进度再排）**

- [ ] 画像 C 的 Fork-first 流程 POC
- [ ] 翻译记忆库 TM 的数据模型设计

### 4. 下个对话应先读哪些文件

**按优先级顺序**：

1. `docs/handoff-20260420.md` ←← **最新交接，必读**
2. `.cursor/rules/project-context.mdc`（技术栈 & 业务对象锁定）
3. `.cursor/rules/vibe-coding.mdc`（协作规范 & 新版交接规则）
4. `docs/PRD.md`（产品全貌，v1.1）

读完这 4 份，新会话可以无缝接上今天的上下文，无需重新解释任何背景。

### 5. 已踩过的坑和妥协

**坑一：工具调用被中断的循环**

- 现象：会话中出现多次 `Read was interrupted by the user after XXXms` 报错，打断了工作流
- 根因推断：一次并发 3+ 个 Read 读同一文件不同片段，可能触发了 IDE 端的挂起/超时
- 补救措施：后续**不再对已熟悉的文件做碎片化并行读取**，改为一次 Read 整个文件 + 内存中定位，或直接用 StrReplace 精确改
- **可迁移经验**：Cursor Agent 被打断后无法自我续跑，必须用户发新消息触发；因此宁可多花一次工具调用做"整块读取"，也别赌并发能省时

**坑二：画像 C 的权限误解**

- 初版 PRD 把画像 C 写成"非付费受益者"，但作者直觉是"画像 C 也可以主动提交翻译"
- 实际 GitHub App 权限模型下不可行——只有仓库所有者能授予 Installation
- 解法：在 PRD 2.3 和 3.1 末尾明确写出**范围说明 + 为什么**，并给 P2 补 Fork-first（F-24）作为画像 C 的合法参与通道
- **可迁移经验**：做第三方平台 SaaS 时，先问"授权方向是谁 → 谁"，再设计角色，否则 PRD 写到一半会推翻重来

**妥协记录**

- **数据最小化**与**调试便利**的权衡：MVP 阶段允许保留原文/译文全文（便于排查 AI 翻译错误），GA 前下线。已在 6.1 安全表中声明
- **队列方案**：MVP 用数据库表模拟队列，不上 Redis/Upstash。省成本，但吞吐上限低，量级上来要换
- **成本模型**：学习期全部用 `:free` 模型，交付体验可能不如付费模型。但符合项目定位（学习 + 起步），可接受

---

## 场次 2（补录 · 今日早些时候的环境准备会话）

> **时序说明**：本场次的工作实际发生在**场次 1 之前**，但当时交接规则还未建立，故在场次 1 之后补录。Chat 原始记录位于 `agent-transcripts/143d80b4-7884-4034-848b-00d1442fa820/`。

### 1. 本次完成了什么

| 操作 | 文件/动作 | 说明 |
| --- | --- | --- |
| 配置 | GitHub App（OAuth + Webhook） | App ID: `1439734`，Client ID: `Iv23liZuUcTmF6FFrSck`；权限开通 Contents RW / PR RW / Metadata Read；订阅 push / installation / installation_repositories 事件 |
| 配置 | GitHub App 测试安装 | 已安装到账号 `tryubest`，Installation ID: `125450380`；Recent Deliveries 有 `installation.created` 记录（405 是预期，因 URL 还是占位） |
| 账号 | OpenRouter | 已创建 API Key，开启 free endpoints；验证过用 `z-ai/glm-4.5-air:free` 调用成功（`cost: 0`） |
| 账号 | Neon Postgres | 创建 project，拿到 `DATABASE_URL`（连接串已在前两次会话中明文出现，**作者决定不 rotate，仅作学习用**） |
| 安装 | pnpm | 通过 `npm install -g pnpm` 安装，版本 10.33.0（corepack enable 因 Program Files 权限不够失败，走的备选） |
| 安装 | cloudflared | 通过 `winget install --id Cloudflare.cloudflared` 安装，版本 2025.8.1（新 PATH 需重开 shell 才生效） |
| 新增 | `.cursor/rules/project-context.mdc` | 项目定位、技术栈锁定、业务对象、目录约定，`alwaysApply: true` |
| 新增 | `.cursor/rules/vibe-coding.mdc` | 协作规范初版（场次 1 后升级为含"主动交接"规则的 v2） |
| 新增 | `.cursor/rules/code-style.mdc` | TypeScript 严格模式、Next.js App Router 约定、命名规范，仅对 `.ts/.tsx` 生效 |
| 新增 | `Document translation/.gitignore` | 子项目级 .gitignore（后来场次 1 又在 `D:/hyd001/` 根目录补了一份更全的） |

### 2. 关键决策与原因

| 决策 | 选择 | 为什么 |
| --- | --- | --- |
| 学习路线结构 | **6 阶段 Vibe Coding 路径**：PRD → 架构 → 登录 → 翻译核心 → Webhook → 部署 | 每阶段产出一份可 commit 的交付物，节奏可控；严格奉行"先方案后代码、小步快跑" |
| 技术栈 | **Next.js 15 App Router + Prisma + Neon + shadcn/ui + OpenRouter + pnpm + Vercel** | 前后端同构部署最简；Vercel 亲爹加持；AI 对这套栈提示词命中率最高；已就业市场主流 |
| 数据库 | **Neon 云 Postgres**（放弃本地 Docker + Postgres） | 零安装、Serverless、后期 Vercel 部署无缝衔接；Windows Docker Desktop 吃内存 |
| 内网穿透 | **cloudflared**（替代 ngrok） | 免费不限时长、URL 稳定；ngrok 免费版有 session 时间限制 |
| 免费模型选择 | **主 `z-ai/glm-4.5-air:free` + 备用 Qwen / gpt-oss** | DeepSeek `:free` 系列 2026 年已从 OpenRouter 下架，GLM 中文母语质量最佳；三备份支持后续"主模型限流自动降级" |
| 密钥配置化 | **模型 ID 走 `.env`，不硬编码** | 切换模型零改代码；是 SaaS 级翻译服务的标配思路 |
| Cursor 规则拆分 | **3 个 `.mdc` 文件：context / vibe-coding / code-style** | 单一职责；前两者 alwaysApply，code-style 仅 `.ts/.tsx` 生效省 token |
| 会话管理策略 | **多对话框 + 规则文件沉淀上下文**（宝物 2 方案） | 单对话长了会被截断遗忘；靠 `.cursor/rules/` 自动注入比每次开场白重复介绍更省心 |

### 3. 遗留事项 / 下一步

本场次结束时的"下一步"为：

- [x] ~~产出 `docs/PRD.md`~~（**已完成**，见场次 1）
- [x] ~~建立 `.cursor/rules/`~~（**已完成**，场次 2 内完成，场次 1 又补充了交接规则）
- [ ] 初始化 Next.js 项目骨架（**下次会话 P0**）：`pnpm create next-app@latest` + Prisma + shadcn/ui
- [ ] 创建 `.env` 并填入凭据（App ID / Private Key / Client Secret / Webhook Secret / OpenRouter Key / Neon URL / Model IDs）
- [ ] 创建 `.env.example` 模板进 git
- [ ] 把 GitHub App 的 `.pem` 私钥放到**项目外**安全位置（推荐 `D:\hyd001\secrets\github-global\`）

### 4. 下个对话应先读哪些文件

**（与场次 1 相同，不重复列出）**

新增要点：`agent-transcripts/143d80b4-.../` 下有本场次原始 chat 记录，如需追溯任何细节（如具体 curl 命令、winget 日志、某个 Prompt 原文），可直接读该 jsonl 文件。

### 5. 已踩过的坑和妥协

| # | 坑 | 根因 | 教训（可迁移） |
| --- | --- | --- | --- |
| 1 | `corepack enable` 失败（EPERM） | Node 装在 `C:\Program Files\`，corepack 需管理员权限写 `yarnpkg` 软链 | Windows 下 Node 装全局 PATH 时优先 `npm install -g pnpm`，不用 corepack |
| 2 | cloudflared 装完命令找不到 | 已存在的 shell 进程不会重新加载 PATH 环境变量 | Windows 装新工具后**必须开新 shell**，这是 Win 通用坑 |
| 3 | `deepseek/deepseek-chat-v3.1:free` 返回 404 | DeepSeek 免费版 2026 年已从 OpenRouter 下架 | **任何教程给的模型 ID 都可能过期**；上线前调 `GET /api/v1/models` 实时核对 |
| 4 | PowerShell `curl -d '{"content":"你好"}'` 中文丢失 | 单引号 + 反斜杠双层转义把中文字符吃掉 | PowerShell 下调 API 用原生 `Invoke-RestMethod` + UTF-8 Bytes；实际项目里 fetch/SDK 不会有此问题 |
| 5 | Webhook Delivery 返回 405 | URL 还是 `example.com` 占位，对方不接受 POST | 这是**预期行为**而非故障；等 cloudflared + Next.js 起来后自然变 200 |

**妥协记录**

- **密钥处理"学习阶段"妥协**：作者决定不 rotate 已暴露的 Neon / OpenRouter 凭据，仅用于本学习项目。**硬约定**：此数据库不能存任何真实数据；上生产前必须用独立 Neon branch + 新密钥
- **Cursor Memory 不用**：讨论过 Cursor 的用户级 Memory 功能，最终选**项目级 Rules**，原因是 Memory 不受版本控制、跨项目污染
- **账号密码管理**：敏感凭据（.pem / 原始 Key）放项目外目录 `D:\hyd001\secrets\`（建议），非敏感 ID 放 `.env.example` 进 git 做模板

---

> 本交接笔记由 AI 按 `.cursor/rules/vibe-coding.mdc` 新版"交接"规则主动生成。下次会话请先读本文件（含所有场次）。
